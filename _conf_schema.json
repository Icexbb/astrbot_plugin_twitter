{
  "basic": {
    "description": "基础设置",
    "type": "object",
    "hint": "Twitter 数据源、Nitter 镜像站、代理与轮询等基础配置。",
    "items": {
      "twitter_data_provider": {
        "description": "Twitter 数据源",
        "type": "string",
        "default": "nitter",
        "options": ["nitter", "fxtwitter"],
        "hint": "nitter=保持原有 Nitter HTML 抓取；fxtwitter=通过 FxTwitter JSON API 获取用户和推文。切换到 fxtwitter 后不会检测或访问 Nitter 镜像。"
      },
      "twitter_fxtwitter_api_base": {
        "description": "FxTwitter API 基础地址",
        "type": "string",
        "default": "https://api.fxtwitter.com",
        "hint": "FxTwitter JSON API 地址，末尾斜杠会自动清理。默认：https://api.fxtwitter.com"
      },
      "twitter_nitter_url": {
        "description": "Nitter 镜像站地址（留空则使用内置列表自动选择）",
        "type": "string",
        "hint": "例如: https://nitter.net",
        "default": ""
      },
      "twitter_proxy": {
        "description": "代理地址（留空则不使用代理）",
        "type": "string",
        "hint": "例如: http://127.0.0.1:7890 或 socks5://127.0.0.1:1080",
        "default": ""
      },
      "twitter_pre_download_media": {
        "description": "通过代理加载图片和较小的视频（需先配置代理地址）",
        "type": "bool",
        "default": false,
        "hint": "配置代理并开启后，插件会预下载推文图片、视频封面和不超过 30MB 的视频，再交给 AstrBot 发送或渲染服务生成截图，失败时回退原链接。关闭后，这些媒体由 AstrBot 或渲染服务从原链接加载。截图头像独立启用小容量本地缓存，始终由插件按代理配置获取，不受此开关影响。"
      },
      "twitter_poll_interval": {
        "description": "推文轮询间隔（分钟）",
        "type": "int",
        "default": 5,
        "hint": "建议不低于 3 分钟，避免频繁请求"
      },
      "twitter_poll_max_tweets_per_user": {
        "description": "每个推主每轮最多推送推文数",
        "type": "int",
        "default": 5,
        "hint": "数据源恢复或积压较多时，每个推主每轮最多按时间顺序推送此数量，剩余推文将在后续轮询继续推送；最小值为 1"
      }
    }
  },
  "message_format": {
    "description": "消息格式",
    "type": "object",
    "hint": "控制推文推送的消息样式、合并转发行为、媒体质量及帖子链接。",
    "items": {
      "twitter_use_node": {
        "description": "单条推文使用合并转发消息发送",
        "type": "bool",
        "default": true
      },
      "twitter_no_text": {
        "description": "推文含媒体时不输出文字内容",
        "type": "bool",
        "default": false
      },
      "twitter_text_render_mode": {
        "description": "推文正文渲染方式",
        "type": "string",
        "default": "text",
        "options": ["text", "screenshot", "both"],
        "hint": "text=普通文字推送；screenshot=使用 AstrBot html_render 渲染 X 风格截图；both=同时发送文本和截图"
      },
      "twitter_screenshot_theme": {
        "description": "截图模式主题",
        "type": "string",
        "default": "dark",
        "options": ["dark", "light"],
        "hint": "dark=黑色背景 X 风格；light=白色背景 X 风格，在 screenshot 和 both 模式下生效"
      },
      "twitter_send_media_separately": {
        "description": "单独发送推文媒体资源",
        "type": "bool",
        "default": true,
        "hint": "开启后会在正文或截图之外继续发送原图和视频；关闭后不再追加原图、视频及视频降级链接，截图中的媒体预览不受影响"
      },
      "twitter_image_quality": {
        "description": "推文图片质量",
        "type": "string",
        "default": "orig",
        "options": ["large", "orig"],
        "hint": "large=缩略图，orig=原图（默认）"
      },
      "twitter_video_max_size_mb": {
        "description": "视频直发大小上限（MB）",
        "type": "int",
        "default": 256,
        "hint": "发送视频前会探测文件大小；超过上限时改为发送说明和视频链接。默认=256"
      },
      "twitter_collective_forward": {
        "description": "轮询时汇总多条新推文为一条合并转发消息",
        "type": "bool",
        "default": false,
        "hint": "仅影响轮询推送。开启后会缓存本轮轮询期间获取到的新推文，轮询结束后统一打包发送；需同时开启“单条推文使用合并转发消息发送”，否则不会生效"
      },
      "twitter_collective_max_authors": {
        "description": "集体转发时单条消息包含的最大推主数",
        "type": "int",
        "default": 5,
        "hint": "超过此数量的推主将分组为多条合并转发消息；仅集体转发模式下生效"
      },
      "twitter_include_tweet_link": {
        "description": "推送消息附带帖子链接",
        "type": "bool",
        "default": true,
        "hint": "开启后消息末尾附带对应 X/Twitter 帖子链接；关闭后轮询推送、推特测试和链接识别解析出的推文消息都不附带帖子链接"
      }
    }
  },
  "content_filter": {
    "description": "内容过滤",
    "type": "object",
    "hint": "控制转帖推送、链接识别等内容策略。",
    "items": {
      "twitter_include_retweets": {
        "description": "轮询推送和推特测试时推送转帖",
        "type": "bool",
        "default": true,
        "hint": "开启后会推送转帖，并在消息中标明谁转发了谁；关闭后轮询跳过转帖，/推特测试会寻找最新的非转贴推文"
      },
      "twitter_deduplicate_retweets": {
        "description": "轮询推送时对转帖去重",
        "type": "bool",
        "default": false,
        "hint": "开启后，同一条原帖被多个订阅推主转发时，每个会话只推送第一次"
      },
      "twitter_link_recognition_enabled": {
        "description": "推文链接解析模式",
        "type": "string",
        "default": "auto",
        "options": ["auto", "off", "command"],
        "labels": ["开启（自动解析）", "关闭", "开启但仅指令触发"],
        "hint": "自动解析会识别聊天中的 twitter.com / x.com 链接；仅指令触发时需使用 /推特解析 <推文链接>；关闭后两种方式均不解析"
      }
    }
  },
  "translation": {
    "description": "翻译设置",
    "type": "object",
    "hint": "配置推文内容的自动翻译行为，需至少一个可用的 LLM Provider。",
    "items": {
      "twitter_translate_enabled": {
        "description": "推文内容翻译开关",
        "type": "bool",
        "default": false,
        "hint": "开启后，推文正文将被翻译为目标语言"
      },
      "twitter_translate_target_lang": {
        "description": "翻译目标语言",
        "type": "string",
        "default": "简体中文",
        "hint": "推文内容将被翻译为此语言，如：简体中文、日语、英语"
      },
      "twitter_translate_provider_id": {
        "description": "翻译使用的 LLM Provider（留空则自动选择）",
        "type": "string",
        "default": "",
        "hint": "选择翻译使用的 Provider，留空时按以下顺序自动选择：当前会话 Provider → 第一个可用 Provider",
        "_special": "select_provider"
      },
      "twitter_translate_timeout_seconds": {
        "description": "单条推文翻译总时限（秒）",
        "type": "int",
        "default": 60,
        "hint": "最小为 1 秒。正文、引用正文和重试共用此时限，超时未完成部分使用原文。轮询中同一 Provider 连续两条推文翻译失败后，本轮剩余内容使用原文，下轮重新尝试；手动测试和解析不参与该计数。"
      },
      "twitter_translate_custom_prompt_enabled": {
        "description": "启用自定义翻译提示词",
        "type": "bool",
        "default": false,
        "hint": "开启后将使用下方自定义的翻译提示词，而非内置默认提示词"
      },
      "twitter_translate_custom_prompt": {
        "description": "自定义翻译提示词",
        "type": "text",
        "editor_mode": true,
        "editor_language": "markdown",
        "default": "你是一个专业的翻译助手。请将用户提供的文本翻译为{target_lang}。\n规则：仅输出翻译结果，不要添加任何解释、前缀、注释或原文对照\n保持原文的语气和格式（如换行、表情符号等）",
        "hint": "自定义翻译 system prompt。支持变量：{target_lang} 会替换为翻译目标语言。"
      }
    }
  }
}
